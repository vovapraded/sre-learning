apiVersion: v1
kind: Service
metadata:
  name: postgres-node
  namespace: metrics
spec:
  clusterIP: None  # Headless service
  selector:
    app: postgres-node
  ports:
    - port: 5432
      targetPort: 5432
